------------------------------------------------------------------------
Záloha - prostá
------------------------------------------------------------------------

# Přepnutí do pracovní složky
cd /home/pi/Projects

# 1. Smazání předchozí verze zálohy na SD kartě
# (Tento příkaz je nevratný!)
sudo rm -rf /mnt/sd_root/web_app_Mediathek

# 2. Zkopírování aktuální verze projektu na SD kartu
sudo cp -r web_app_Mediathek /mnt/sd_root/

# 3. Kontrola nově vytvořené zálohy
ls -l /mnt/sd_root/web_app_Mediathek

------------------------------------------------------------------------
Záloha - synchronizace - rychlé

# Není potřeba 'cd', používáme absolutní cesty
# Synchronizuje zdroj do cíle.
# 'rsync' vytvoří složku 'web_app_Mediathek' v '/mnt/sd_root/', pokud neexistuje.

sudo rsync -av --delete /home/pi/Projects/web_app_Mediathek /mnt/sd_root/

# 3. Kontrola zálohy (zůstává stejná)
ls -l /mnt/sd_root/web_app_Mediathek

------------------------------------------------------------------------
